{
  "name": "inkandarch-timetracking",
  "version": "1.0.0",
  "description": "Time tracking application with MongoDB and SQLite support",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "npm run setup && node server.js",
    "dev:frontend": "python3 -m http.server 3000",
    "build": "echo 'Static site - no build needed'",
    "setup": "node -e \"const fs = require('fs'); if (!fs.existsSync('data')) fs.mkdirSync('data', {recursive: true});\" && npm install --production"
  },
  "keywords": ["time-tracking", "employee-management", "medical-clinic", "mongodb", "sqlite"],
  "author": "Ink and Arch",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^6.3.0",
    "sqlite3": "^5.1.6",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "type": "commonjs"
}
